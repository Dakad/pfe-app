

each type in Object.keys(messages)
	each msg in messages[type]
		.row.clearfix
			div.alert.alert-dismissible(class='alert-'+type)
				button.close(type="button" data-dismiss="alert") ×
				p=msg

